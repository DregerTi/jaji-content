<form method="post" action="{{ path('back_contents_delete', {'slug': content.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ content.id) }}">
    <button class="btn">Delete</button>
</form>
