<a class="item flex flex-row gap-1 cursor-pointer mt-5 mb-5" href="{{ path(path, pathParams) }}">
    <article class="flex flex-col gap-2">
        {% if icon is defined %}
            <i data-feather="{{ icon }}"></i>
        {% endif %}
        <div class="flex flex-col gap-0.5">
            <label class="text-black font-medium text-lg">{{ name }}</label>
            <div class="text-grey text-base flex flex-col">
                {% if createdAt is defined %}
                    <p>Crée le
                        <date>{{ createdAt }}</date>
                        par
                        {{ createdBy }}
                    </p>
                {% endif %}
                {% if updatedAt is defined %}
                    <p>Modifié le
                        <date>{{ updatedAt }}</date>
                        par
                        {{ updatedBy }}
                    </p>
                {% endif %}
                {% if status is defined %}
                    <div class="status-box">{{ status }}</div>
                {% endif %}
            </div>

        </div>
    </article>
</a>
