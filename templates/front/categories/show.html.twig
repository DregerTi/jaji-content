{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <h1>Categories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ category.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_categories_index') }}">back to list</a>

    <a href="{{ path('back_categories_edit', {'id': category.id}) }}">edit</a>

    {{ include('back/categories/_delete_form.html.twig') }}
{% endblock %}
